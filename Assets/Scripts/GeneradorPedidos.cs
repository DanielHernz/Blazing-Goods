using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GeneradorPedidos : MonoBehaviour
{
    private int NuProductos;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Generar numero random de 1 a 3 (NuProductos)
        //Escoger numero random; 0 a 2 (cajas)
        //Escoger numero random; 6 o 7 (etiquetas)
        //Traer etiqueta correspondiente al numeror random
        //Traer la caja que corresponda con el numero
        //Crear array con el numero de elementos que lance numero random + la caja correspondiente + burbuja + envoltura + valor etiqueta
        //Registrar pedido
    }
}
