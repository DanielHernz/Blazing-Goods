using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DelayRapido : MonoBehaviour
{
    public bool DRFinish = false;
    public bool DRActivado = false;
    private float MaxTime = 3;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if {DRActivado == true}
            //Iniciar contador
            //if {MaxTime => 3}
                //public bool DRFinish = true;

        //public bool DRActivado = false;
    }
}
