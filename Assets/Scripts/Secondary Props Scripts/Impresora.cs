using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Impresora : MonoBehaviour
{
    public bool SwithEtiquetaNA = true;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Asignar GameObject fuente
        //if {SwitchEtiquetaNA == false}
            //Asignar GameObject EtiquetaINT para instanciar
        //Asignar GameObject EtiquetaNA para instanciar
        //if {PlayerControl.ManosVacias = true}
            //if {oncollisionenter con collider hijo de player}
                //if {mouse down}
                    //DelayLento.DLFinish == true
                        //Instanciar GameObject asignado "EtiquetaX"
                        //Convertir GameObject en hijo de Player
    }
}
