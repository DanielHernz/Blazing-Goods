using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SwitchEtiqueta : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if {Impresora.SwithEtiquetaNA == true}
            //Cambiar color switch rojo
                //if {oncollisionenter con collider hijo de player}
                    //if {mouse down}
                        //SwitchEtiquetaNA = False
        //if {Impresora.SwithEtiquetaNA == false}
            //Cambiar color switch azul
                //if {oncollisionenter con collider hijo de player}
                    //if {mouse down}
                        //SwitchEtiquetaNA = true
    }
}
