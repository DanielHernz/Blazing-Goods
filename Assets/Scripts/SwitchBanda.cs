using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SwitchBanda : MonoBehaviour
{
    private float VelocidadBanda;
    private bool DerechaON = true;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if {DerechaON == true}
            //for {Usar VelocidadBanda y Mover banda en dirección +X}
        //elif {DerechaON == false}
            //for {Usar VelocidadBanda y Mover banda en dirección -X}
    }
}
