using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PosarProp : MonoBehaviour
{
    public bool ObjetoON = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Habilitar script "Dispensar"
        //Get own position
        //if {oncollisionenter Collider hijo de Player con Collider de Dispensador}
            //if {Mouse down}
                //Transferir prop hijo a dispensador
                //ObjetoON = true
                //Deshabilitar script "Dispensar"
    }
}
